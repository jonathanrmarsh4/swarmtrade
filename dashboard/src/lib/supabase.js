// Supabase client — initialised with SUPABASE_URL and SUPABASE_SERVICE_KEY from process.env.
// Exports the configured client and any shared real-time subscription helpers.
// All dashboard components import from here — never instantiate a second client elsewhere.
